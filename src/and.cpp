#include <iostream>
#include "and.h"
#include <vector>

using namespace std;

void And::execute()
{
    /*Command after && will only execute if leftChild is successful

    if(leftChild->execute == SUCCESS)
    {
        rightChild->execute;
    }

    */
}
