#include <iostream>
#include <vector>
#include "or.h"

using namespace std;

void Or::execute()
{
    /*rightChild of || only executes if leftChild fails.

    if(leftChild->execute() == FAIL)
    {
        rightChild->execute();
    }
    else

    exit(fail);
    */
}
